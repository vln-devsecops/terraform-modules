# Example: aws/deployment_bucket

This example shows the minimal usage of the shared deployment bucket module.

Run from this directory with appropriate AWS credentials if you want to plan or apply the example.
