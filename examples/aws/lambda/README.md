# Example: aws/lambda

This example shows the shared Lambda module with an explicit deployment archive key, Function URL support, and a small environment map.

Run from this directory with appropriate AWS credentials if you want to plan or apply the example. To mirror the current `docxchange` archive naming contract, omit `source_object_key` and let the module default to `app_name-function_name.zip`.
